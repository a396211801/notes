指标 			MongoDB(v2.4.9) 		Redis(v2.4.17) 				比较说明

性能			依赖内存，TPS较高		依赖内存，TPS非常高			Redis优于MongoDB

可操作性		丰富的数据表达、索引；
			最类似于关系数据库，支持	数据丰富，较少的IO			MongoDB优于Redis
			丰富的查询语言		

内存及存储		适合大数据量存储，依赖		Redis2.0后增加虚拟内存特性，
			系统虚拟内存管理，采用镜像	突破物理内存限制；数据可以		不同的应用角度看，各有优势
			文件存储；内存占有率比较高，	设置时效性，类似于memcache
			官方建议独立部署在64位系统
			（32位有最大2.5G文件限制，
				64位没有改限制）		


可用性			支持master-slave,replicaset	依赖客户端来实现分布式读写；		MongoDB优于Redis；单点问题上，
			（内部采用paxos选举算法，自	主从复制时，每次从节点重新连接		MongoDB应用简单，相对用户透明
			动故障恢复）,auto sharding机制	主节点都要依赖整个快照,无增量复制；	，Redis比较复杂，需要客户端
			，对客户端屏蔽了故障转移和切分	不支持自动sharding,需要依赖程序设定	主动解决。（MongoDB一般会使用
			机制				一致hash机制				replica sets和sharding功能结合，
												replica sets侧重高可用性及高可靠性，而														sharding侧重于性能、易扩展）



可靠性			从1.8版本后，采用binlog方式	依赖快照进行持久化；
			（MySQL同样采用该方式）支持持	AOF增强可靠性；				MongoDB优于Redis
			久化，增加可靠性		增强可靠性的同时，影响访问性能	

一致性			不支持事物，靠客户端自身保证	支持事物，比较弱，仅能保证事物中	Redis优于MongoDB
							的操作按顺序执行

数据分析		内置数据分析功能（mapreduce）		不支持				MongoDB优于Redis


应用场景		海量数据的访问效率提升		较小数据量的性能及运算			MongoDB优于Redis



总结：上面通过我在查询资料是分析出来的信息。。
	也因通过最近都在了解mongo和redis也能知道上面的分析对于这两个存储数据库的性能相差已经不大。。	
	基本性能上都已经有其对比。。
	所以针对我们开会时所面临的问题主要可以通过上面的几个大的方向对比（可操作性、内存及存储、可用性、数据分析、应用场景）。
	我们主要针对数据的存储方案、和后面操作的性能优化成报表的情况下。
	所以可以优先考虑mongo.
	而mongo对于我们没有接触过的数据库存储上也算是比较简单易懂。算是很快容易上手。

		